jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Install
          command: npm install
      - run:
          name: Publishing
          command: lerna publish --conventional-commits -y
